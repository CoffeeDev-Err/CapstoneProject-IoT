I want you to redesign and improve the UI/UX of this project so it looks polished, modern, professional, and production-ready.

PROJECT:
BantayCabagan – An IoT-Based Real-Time GPS Monitoring System for Police Personnel.

MAIN GOAL:
Improve the visual design and user experience of the existing system while preserving its current content, functionality, backend integrations, routes, database logic, and overall workflow.

The system's content and features are already complete.

Do NOT invent unnecessary new dashboard sections, features, statistics, or content.

Your main responsibility is to improve how the existing system LOOKS, FEELS, and BEHAVES from a UI/UX perspective.

==================================================
DESIGN DIRECTION
================

Create a modern public-safety / police monitoring system style.

The interface should feel:

- Professional
- Trustworthy
- Organized
- Clean
- Modern
- Secure
- Suitable for an actual police station or local government environment

It should NOT feel:

- Like a gaming website
- Overly futuristic
- Flashy
- Neon-heavy
- Overdesigned
- Like a generic AI-generated dashboard template

Use a consistent visual language throughout the application.

==================================================
DESIGN STYLE
============

Use:

- Modern SaaS/admin dashboard design principles
- Clean and minimal layouts
- Proper whitespace
- Clear visual hierarchy
- Consistent spacing
- Modern typography
- Professional icons
- Soft shadows
- Subtle borders
- Properly sized border radiuses
- Clean cards and containers
- Smooth but subtle animations
- Responsive layouts
- Consistent sizing between similar components

Avoid:

- Excessive gradients
- Excessive shadows
- Oversized cards
- Oversized headings
- Too many rounded elements
- Too many floating elements
- Random colors
- Neon colors
- Unnecessary animations
- Excessive glassmorphism
- Putting every single element inside a card
- Decorative elements that do not improve usability

The result should feel intentional and professionally designed.

==================================================
COLOR DIRECTION
===============

Use a professional navy/blue-based visual identity appropriate for a police monitoring system.

Suggested direction:

- Dark navy for sidebar and major navigation
- Professional blue for primary actions and highlights
- White or light gray for main surfaces
- Neutral gray for secondary information
- Green for online, safe, success, active, or normal states
- Yellow/orange for warnings
- Red only for errors, danger, SOS, critical alerts, or serious violations

Do not overuse status colors.

Maintain proper contrast and accessibility.

==================================================
IMPORTANT: PRESERVE EXISTING CONTENT
====================================

The existing dashboard content, pages, sections, information, and features are already complete.

Do NOT:

- Add unnecessary dashboard sections
- Replace existing information with different content
- Remove useful information
- Invent new statistics
- Invent new features
- Replace real data with mock data
- Change existing workflows without a strong UX reason
- Redesign the information architecture unnecessarily

Preserve the existing information hierarchy and purpose of each section.

You may slightly reposition, resize, regroup, or reorganize UI elements only when it clearly improves:

- Readability
- Alignment
- Spacing
- Responsiveness
- Accessibility
- Usability

Do not change the meaning or purpose of existing content.

==================================================
VERY IMPORTANT: DO NOT REDESIGN EVERYTHING AT ONCE
==================================================

Do NOT modify the entire project in one large operation.

Before editing anything, inspect the project and divide the redesign into clear phases.

For example, the phases may look similar to:

Phase 1 – Global Design System
Phase 2 – Login / Authentication
Phase 3 – Main Layout / Sidebar / Header
Phase 4 – Dashboard
Phase 5 – GPS / Live Map Interface
Phase 6 – Personnel Management
Phase 7 – Alerts / Notifications
Phase 8 – Reports / Records
Phase 9 – Settings / Profile
Phase 10 – Mobile Responsiveness and Final Consistency Review

These are examples only.

Create the actual phases based on the existing project structure.

IMPORTANT:

Do NOT start implementing all phases immediately.

First:

1. Inspect the project.
2. Understand the existing pages and components.
3. Identify the current design system or styling approach.
4. Identify reusable components.
5. Identify visual inconsistencies.
6. Identify responsive issues.
7. Identify duplicate styles/components.
8. Divide the redesign into logical phases.
9. Present the proposed redesign plan.

Then STOP.

Do not implement anything yet until the redesign plan is clear.

==================================================
IMPLEMENTATION WORKFLOW
=======================

After the redesign plan is established, work on ONLY ONE PHASE AT A TIME.

For each phase:

1. Inspect all relevant files first.
2. Briefly identify what needs visual improvement.
3. Explain what you plan to change.
4. Modify only the files needed for that phase.
5. Preserve all existing functionality.
6. Review the result for consistency.
7. Check responsiveness.
8. Check for obvious UI bugs.
9. Check that backend/API functionality was not affected.
10. Remove unused code introduced during the changes.

After completing that phase:

STOP.

Do NOT automatically continue to the next phase.

Give me a short summary containing:

- What you changed
- Which files were changed
- Any important design decisions
- Any issues you noticed
- What the next proposed phase is

Then wait for me to review it before continuing.

Do not redesign multiple major sections at the same time.

==================================================
GLOBAL DESIGN SYSTEM
====================

Before repeatedly styling individual pages independently, establish a consistent reusable design system.

Identify or create reusable standards for:

- Colors
- Typography
- Font sizes
- Font weights
- Spacing
- Border radius
- Shadows
- Borders
- Buttons
- Form inputs
- Cards
- Tables
- Modals
- Dropdowns
- Tooltips
- Badges
- Alerts
- Toast notifications
- Loading indicators
- Skeleton loaders
- Empty states
- Error states
- Icons

Use consistent design tokens or reusable styles/components whenever appropriate.

Avoid hardcoding slightly different versions of the same styles across many files.

==================================================
REUSABLE COMPONENTS
===================

Improve or create reusable components when appropriate for:

- Sidebar
- Top navigation/header
- Mobile navigation
- Buttons
- Form inputs
- Password inputs
- Search bars
- Select/dropdown controls
- Cards
- Statistics cards
- Tables
- Status badges
- Alert components
- Confirmation dialogs
- Modal windows
- Loading states
- Empty states
- Error states
- Toast notifications
- Pagination
- Filters
- Date/time controls
- Tabs

Do not create abstractions unnecessarily.

Only extract reusable components when they genuinely improve maintainability.

==================================================
SIDEBAR AND NAVIGATION
======================

The navigation should look clean and professional.

Improve:

- Spacing
- Active states
- Hover states
- Icons
- Alignment
- Section grouping
- Collapsed state if supported
- Mobile behavior
- Visual hierarchy

The active page should be immediately recognizable.

Do not make the sidebar overly wide.

Avoid excessive decorative elements.

==================================================
HEADER / TOP NAVIGATION
=======================

Keep the header compact and useful.

Properly organize existing elements such as:

- Page title
- Search
- Notifications
- Profile
- Status indicators
- Actions

Do not waste vertical screen space.

Make sure mobile behavior is handled properly.

==================================================
DASHBOARD
=========

The dashboard's existing content and information are already complete.

Do NOT add or remove dashboard content unless absolutely necessary.

Do NOT invent a new hierarchy of information.

Preserve the existing dashboard structure and data.

Focus on improving:

- Visual design
- Layout
- Spacing
- Alignment
- Typography
- Cards
- Containers
- Icons
- Status indicators
- Colors
- Contrast
- Responsive behavior
- UI consistency
- Subtle interactions

You may slightly reposition or resize elements only when it clearly improves usability or responsiveness.

The dashboard should look like a real operational monitoring system, not a school-project template.

==================================================
LIVE GPS MAP
============

The live map is an important part of BantayCabagan.

Preserve its current features and functionality.

Improve only the interface surrounding it.

Make sure:

- The map has enough visible space
- Controls remain compact
- Controls do not unnecessarily cover the map
- Personnel markers are understandable
- Status indicators are clear
- Filters are easy to use
- Selected personnel information is readable
- Information panels are organized
- Alert states are visible but not visually overwhelming
- Map controls work well on smaller screens

Do not reduce the usability of the map for decorative design purposes.

==================================================
TABLES
======

Improve existing tables using:

- Clean row spacing
- Clear headers
- Proper alignment
- Readable typography
- Consistent column spacing
- Status badges
- Search/filter areas
- Pagination if already implemented
- Proper empty states
- Loading states
- Mobile responsiveness

Avoid overly dense tables.

Avoid excessive borders.

For smaller screens, determine the best responsive behavior instead of simply shrinking every column.

==================================================
FORMS
=====

Improve forms with:

- Clear labels
- Proper placeholders
- Helper text where needed
- Consistent input heights
- Proper spacing
- Clear required-field indication
- Clear validation errors
- Password visibility controls where appropriate
- Loading states
- Disabled submit state while processing
- Clear success and error feedback

Do not rely only on placeholder text as a field label.

Group related fields logically.

==================================================
AUTHENTICATION PAGES
====================

Create a premium, clean, secure authentication experience.

Maintain the existing authentication functionality.

This may include existing flows such as:

- Login
- OTP verification
- Two-step authentication
- Forgot password
- Reset password
- Change password

The design should feel official and trustworthy.

Use:

- BantayCabagan branding
- Clear system name
- Professional subtitle if one already exists or is appropriate
- Clean authentication container
- Clear labels
- Password visibility controls
- Proper error feedback
- Loading states
- Disabled states
- Responsive behavior

Avoid oversized login cards.

Avoid unnecessary graphics.

Avoid making it look like a commercial gaming or crypto login page.

==================================================
STATUS INDICATORS
=================

Statuses such as:

- Online
- Offline
- Active
- Inactive
- On Duty
- Off Duty
- Warning
- Critical
- Verified
- Pending

should have a consistent design.

Use:

- Small badges
- Icons where helpful
- Clear labels
- Appropriate colors

Do not communicate important information through color alone.

==================================================
MODALS AND DIALOGS
==================

Use professional modal designs.

Make sure:

- Titles are clear
- Descriptions are concise
- Actions are obvious
- Cancel and destructive actions are distinguishable
- Dangerous actions require proper confirmation
- Mobile layouts remain usable

Do not create oversized dialogs for simple actions.

==================================================
LOADING STATES
==============

Avoid blank pages while data is loading.

Where appropriate, use:

- Spinners
- Skeleton loaders
- Button loading indicators
- Table loaders
- Map loading states

Loading states should be subtle and consistent.

==================================================
EMPTY STATES
============

When no data exists, show a useful empty state instead of an empty container.

Keep empty states simple.

Explain:

- What is missing
- Why the section may be empty
- What action can be taken if applicable

Do not use large decorative illustrations unless they genuinely improve the experience.

==================================================
ERROR STATES
============

Errors should be understandable.

Avoid exposing raw technical errors to normal users.

Use clear messages for:

- Failed API requests
- Login errors
- Invalid credentials
- OTP errors
- Network issues
- GPS/device connectivity issues
- Form validation problems

Preserve technical details for logs or development tools where appropriate.

==================================================
MICRO-INTERACTIONS
==================

Add subtle, professional interactions such as:

- Button hover states
- Button pressed states
- Focus states
- Sidebar transitions
- Dropdown transitions
- Modal transitions
- Toast notifications
- Loading indicators
- Selected-row states
- Meaningful card hover states

Animations should feel:

- Fast
- Smooth
- Subtle
- Purposeful

Avoid animations that delay normal use.

==================================================
RESPONSIVE DESIGN
=================

The system should work properly on:

- Large desktop screens
- Laptops
- Tablets
- Mobile phones

Do NOT simply shrink the desktop UI.

Adapt components properly.

Examples:

- Sidebar may collapse or become a drawer
- Tables may require alternative mobile layouts
- Forms should become properly stacked
- Map controls should remain usable
- Action groups may wrap or collapse
- Cards should resize intelligently
- Modals should fit smaller screens
- Navigation should remain accessible

Avoid horizontal overflow unless absolutely necessary.

==================================================
ACCESSIBILITY
=============

Maintain good accessibility practices.

Include:

- Good color contrast
- Readable font sizes
- Appropriate button sizes
- Clear focus states
- Keyboard-friendly controls where applicable
- Proper labels
- Clear validation messages
- Icons with understandable meaning
- Text labels for important statuses

Do not rely only on color to communicate important information.

==================================================
CODE QUALITY
============

Follow the project's existing:

- Framework
- Architecture
- Folder structure
- Coding conventions
- State management
- Styling approach

Do not introduce unnecessary frameworks or dependencies.

Before installing a new package, determine whether the existing project can already achieve the same result.

Avoid:

- Duplicated UI code
- Giant components
- Unnecessary abstractions
- Hardcoded repeated styling
- Unrelated refactors
- Rewriting working logic without a reason
- Changing API contracts unnecessarily
- Changing database structures for visual design changes

Keep the code:

- Clean
- Reusable
- Organized
- Maintainable
- Easy to understand

==================================================
FUNCTIONALITY SAFETY
====================

UI redesign must NOT break:

- Authentication
- Authorization
- Backend APIs
- MongoDB integration
- GPS functionality
- Real-time updates
- WebSocket/socket functionality if present
- Alerts
- Notifications
- Geofencing
- Personnel monitoring
- Reporting
- Existing routing
- Existing forms
- Existing mobile functionality

Do not change backend logic unless it is genuinely required for the UI feature being improved.

If you discover a backend problem while redesigning, explain it first instead of silently making large backend changes.

================
MOCK DATA / TEST DATA
=====================

The project is currently still in development and the physical GPS device and Flespi integration are not yet fully configured.

Because of this, mock/test GPS and personnel location data MAY be used when needed to test features that depend on real-time location data.

Mock data is allowed specifically for development and testing purposes, including:

- GPS location simulation
- Personnel movement simulation
- Geofence entry
- Geofence exit
- Geofence violation detection
- Alert generation
- Real-time map marker movement
- Online/offline GPS device states
- Location history
- Duty monitoring
- Notifications
- Other features that cannot yet be tested using the physical GPS device

IMPORTANT:

Mock data must be clearly separated from production/real GPS data.

Do not permanently replace the real GPS/Flespi integration architecture with mock data.

The system should be designed so that mock data can later be replaced by real GPS data with minimal changes.

Prefer using a dedicated development/testing mechanism such as:

- Mock GPS service
- Test location generator
- Development-only API endpoint
- Simulation mode
- Test personnel records
- Environment-based configuration

Avoid scattering hardcoded latitude/longitude values throughout the application.

Centralize mock GPS data or simulation logic whenever possible.

If a feature already has a real backend/data integration, preserve it unless changes are necessary.

For GPS-dependent features:

1. Determine whether real GPS/Flespi data is currently available.
2. If not available, use controlled mock/test data.
3. Clearly identify which part of the implementation is simulated.
4. Keep the implementation compatible with future real GPS/Flespi integration.
5. Do not redesign the system around mock data only.

When testing movement, use realistic location changes rather than random unrelated coordinates.

Mock personnel should be able to simulate scenarios such as:

- Staying inside a geofence
- Entering a geofence
- Leaving a geofence
- Moving between locations
- Going offline
- Coming back online
- Triggering a geofence alert

Where possible, make the simulation controllable so developers can intentionally trigger specific scenarios.

For example:

TEST / SIMULATION MODE

Personnel A:
Inside Geofence

Personnel B:
Moving toward boundary

Personnel C:
Outside Geofence

Personnel D:
GPS Offline

This is development-only functionality and should not be confused with real operational police tracking data.

When the physical GPS device and Flespi integration become available, the system should be able to transition from simulated data to real device data without requiring a major rewrite.

FINAL DESIGN STANDARD
=====================

The finished product should look like a real system that could be deployed and demonstrated to:

- Police personnel
- A police station
- A local government unit
- System evaluators
- Capstone panelists

It should NOT look like:

- A basic student template
- A generic Bootstrap dashboard
- A randomly generated AI interface
- A gaming dashboard
- A flashy concept design that sacrifices usability

Aim for the quality and polish of a modern professional admin/SaaS application while maintaining the identity and purpose of BantayCabagan.

==================================================
FIRST TASK
==========

DO NOT start redesigning the whole system yet.

For your first response:

1. Inspect the project structure.
2. Inspect the existing UI.
3. Identify the major pages.
4. Identify shared/reusable components.
5. Identify the existing styling approach.
6. Identify major UI/UX inconsistencies.
7. Identify responsive issues.
8. Divide the redesign into logical phases.
9. Recommend which phase should be completed first.
10. Tell me which files you expect to modify during that first phase.

Do NOT implement multiple phases.

After presenting the plan, STOP and wait for my approval before beginning the first redesign phase.
