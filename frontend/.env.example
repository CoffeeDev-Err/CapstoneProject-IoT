# The local development defaults already point to http://localhost:4000.
# These are optional when the production frontend and API use the same domain.
VITE_API_URL=http://localhost:4000
VITE_SOCKET_URL=http://localhost:4000
