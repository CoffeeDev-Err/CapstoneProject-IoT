# Local Vite proxy target. Use AWS to share data with the installed mobile APK.
# For a local backend, set this to http://localhost:4000 instead.
DEV_API_PROXY_TARGET=https://geosentri.com
# Production defaults to the website origin. Override only for a separate API.
VITE_API_URL=
VITE_SOCKET_URL=
VITE_MAPTILER_API_KEY=replace_with_your_web_maptiler_key
